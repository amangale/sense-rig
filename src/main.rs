mod pipeline;
mod source;
mod stats;
mod types;

fn main() {
    println!("sense-rig 0.1.0 — pipeline logic phase (async wiring lands in phase 3)");
    println!("Run `cargo test` to see the delivery-semantics verdicts in action.");
}